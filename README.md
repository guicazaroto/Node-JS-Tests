# Node-JS-Tests

### Para rodar o projeto rode:

```
>> docker-compose up -d
>> yarn start
>> yarn test
````

### Os testes estão todos na pasta:
```
/src/tests
  - routes.test.js
  - UserController.test.js
```
